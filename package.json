{
  "name": "agent-surface-root",
  "private": true,
  "packageManager": "yarn@4.12.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace agent-surface build",
    "typecheck": "yarn workspace agent-surface typecheck",
    "test": "yarn workspace agent-surface test"
  }
}
